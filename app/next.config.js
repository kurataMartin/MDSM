/** @type {import('next').NextConfig} */
const nextConfig = {
  // ── Remove the Next.js development indicator (the black "N" button
  //    in the bottom-left corner of the screen in dev mode).
  //    This only affects development — has zero effect in production.
  devIndicators: false,
}

module.exports = nextConfig